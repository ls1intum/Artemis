import { Meta, Subtitle, Title } from '@storybook/addon-docs/blocks';
import * as IntroductionStories from './introduction.stories';
import { tumUiGuideUrl } from '../.storybook/preview-guide-url';

<Meta of={IntroductionStories} />

<Title />

<Subtitle>Interactive reference for the reusable Angular components maintained in the TUM UI package.</Subtitle>

Use the sidebar to browse component states, controls, accessibility results, and generated API
documentation. Ownership, architecture, contribution rules, and local commands are documented in the <a href={tumUiGuideUrl} target="_top">TUM UI package guide</a>.
