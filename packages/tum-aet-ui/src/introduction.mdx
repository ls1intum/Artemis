import { Meta, Subtitle, Title } from '@storybook/addon-docs/blocks';
import { tumAetUiGuideUrl } from '../.storybook/preview-guide-url';
import * as IntroductionStories from './introduction.stories';

<Meta of={IntroductionStories} />

<Title />

<Subtitle>Angular components from the Applied Education Technologies research group at TUM.</Subtitle>

TUM AET UI is used by Artemis. Use the sidebar to inspect supported states, controls,
accessibility results, and generated API documentation. Architecture, contribution rules, and
local commands are documented in the <a href={tumAetUiGuideUrl} target="_top">TUM AET UI package guide</a>.
