import Callout from '../../../src/components/Callout/Callout';
import { CalloutVariant } from '../../../src/components/Callout/Callout.types';

# Client Theming

Artemis supports light and dark themes. The active scheme is exposed as
`data-theme="light"` or `data-theme="dark"` on the document element so application styles, TUM UI,
and embedded content share one framework-neutral signal.

<Callout variant={CalloutVariant.danger}>
    Verify every visual change in light and dark mode. Do not hard-code colors in component stylesheets, templates, or TypeScript. Do not introduce Bootstrap, PrimeNG, or
    package-owned color properties as a shortcut.
</Callout>

For design intent and accessibility requirements, follow the
[AET UI/UX color guidelines](https://ls1intum.github.io/ui-ux-guidelines/docs/colors). This page
documents how Artemis implements those decisions.

## Theme architecture

The theming boundary has four layers:

1. The light and dark SCSS variable files define Artemis semantic roles and compile them to CSS
   custom properties.
2. `src/main/webapp/tailwind.css` exposes selected roles as semantic Tailwind utilities.
3. The same file maps Artemis foundations and color roles to the public `--tumaet-ui-*` contract.
   TUM UI components consume only that package-owned contract.
4. Bootstrap and PrimeNG receive compatible values while they remain in the application. They are
   migration consumers, not the source of Artemis design decisions.

This keeps one semantic source of truth while allowing legacy frameworks to leave independently.
Do not reference Artemis properties from package source, and do not reference `--tumaet-ui-*`
directly from application components. Change the host mapping when a package-wide theme decision
changes.

### Three color vocabularies coexist; application code uses one

While the migration runs, three separate systems can name a color in a template, and they switch on
different signals:

| Vocabulary              | Looks like                                                                           | Switches on                                         | Status in application code                                                    |
| ----------------------- | ------------------------------------------------------------------------------------ | --------------------------------------------------- | ----------------------------------------------------------------------------- |
| Artemis semantic layer  | `text-state-danger`, `var(--module-bg)`, `var(--body-color)`                         | the dark theme stylesheet, loaded asynchronously    | **the vocabulary to use**                                                     |
| PrimeUI Tailwind plugin | `text-muted-color`, `text-color`, `bg-emphasis`, `rounded-border`, `border-surface*` | the PrimeNG dark-theme attribute, set synchronously | legacy carry-over; do not add new uses                                        |
| TUM UI theme contract   | `--tumaet-ui-*`                                                                      | `data-theme`, set synchronously                     | package-internal; reachable from an application only through the host mapping |

<Callout variant={CalloutVariant.warning}>
    Do not mix vocabularies within one element or one template. The three switch at different moments during a theme change and on a cold load, so a mixed element renders
    half-switched — a dark border around a light fill — until the last of them catches up. Cloudscape states the same rule as [not mixing components from different visual
    modes](https://cloudscape.design/foundation/visual-foundation/visual-modes/) in one application.
</Callout>

Two consequences worth knowing before you reach for a class name:

- **The PrimeUI utilities are not Artemis design tokens.** They agree with the Artemis roles today
  only because two files are kept in step by hand. Where that hand-maintenance stopped — the primary
  color in light mode, and every state color — they disagree. Replace them as you touch a template:
  a TUM UI component input first, then a semantic Tailwind utility, then an Artemis semantic
  property.
- **TUM UI's own Tailwind class names do not exist in the application bundle.** The package compiles
  its theme into its prefixed build, so `text-muted`, `bg-content-background` and `border-border`
  generate no rule in Artemis and fail silently. Reaching for them looks correct and renders nothing.

## Choosing styles

Use the first suitable option:

1. **TUM UI component input or variant.** Use `severity`, `variant`, `size`, and other supported
   contracts instead of overriding component internals.
2. **Semantic Tailwind utility.** Use one when Artemis owns the mapping, such as
   `text-state-danger`. PrimeUI surface scales are not Artemis design tokens.
3. **Artemis semantic CSS property.** Use an existing role in component SCSS or an irreducible
   dynamic style when a utility cannot express the requirement.
4. **New semantic role.** Add one only when the concept recurs and no existing role has the correct
   meaning. Define both light and dark values and expose it to TUM UI only when it is a supported
   package customization axis.

<Callout variant={CalloutVariant.warning}>
    A raw palette value describes appearance, not intent. Do not use hexadecimal values, numbered Tailwind colors, `--p-*`, or `--bs-*` in new component code. Reusing an unsuitable
    token because its current color looks right is also incorrect.
</Callout>

Common Artemis roles include:

| Intent                       | Artemis role                                                              |
| ---------------------------- | ------------------------------------------------------------------------- |
| primary action and link      | `--primary`, `--primary-dark`                                             |
| default and muted text       | `--body-color`, `--text-body-secondary`                                   |
| content and overlay surfaces | `--module-bg`, `--popover-bg`, `--hover-slightly-darker-body-bg`          |
| border                       | `--border-color`                                                          |
| state                        | `--danger`, `--success`, `--warning`, `--info`                            |
| state message                | `--artemis-alert-{danger,success,warning,info}-{color,background,border}` |

Use the `state-*` Tailwind utilities for state-colored plain markup:

```html
<small class="text-state-danger" id="title-error"> {{ 'entity.validation.required' | artemisTranslate }} </small>
```

Use TUM UI for a component-shaped status:

```html
<tum-ui-message severity="error"> {{ 'entity.validation.required' | artemisTranslate }} </tum-ui-message>
```

Color must never be the only signal. Pair a state color with text, an icon, or another programmatic
cue as required by
[WCAG 1.4.1](https://www.w3.org/WAI/WCAG22/Understanding/use-of-color.html).

### State roles are fills; check the pairing before using one as a foreground

`--danger`, `--success`, `--warning`, and `--info` are the saturated fill colors of a state. They are
readable as a foreground only where the surface allows it, and light mode is where they fail: on a
white card, `--warning` measures 1.63:1 and `--info` 3.04:1 — both below the 3:1 that
[WCAG 1.4.11](https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html) requires even of a
non-text icon. Only `--danger` clears 4.5:1 as body text.

For state-colored **text or icons**, use a TUM UI component with a `severity`, which resolves the
dark-corrected foreground role, or the `--artemis-alert-{state}-color` property directly. Reserve
`text-state-*` for a fill, a border, or a foreground whose pairing you have measured. Contrast is a
property of the pairing, not of the token: the same muted foreground passes on a card surface and
fails on the slightly darker emphasis surface.

### Fixed chrome opts out of surface-derived properties

A region painted the same color in both themes — the navigation bar, editor chrome — is not the page
surface, so every property derived from the page surface resolves against the wrong background there.
The navbar is `--navbar-bg` in both themes, and a component placed inside it that resolves a muted
foreground against the page surface measures 2.31:1 in light mode. Such a region sets its own
foreground properties (`--navbar-dark-color` and the scoped `--tumaet-ui-*` overrides beside it),
which every other navbar item already does. Scoping `data-theme` onto the region is not a substitute:
the host mapping in `tailwind.css` is unlayered and outranks the package's own theme block, so a
scoped attribute flips only the properties the host does not name.

## Adding or changing a theme role

Before adding a property, search the existing semantic roles and the
[TUM UI theme contract](/developer/guidelines/tum-ui-kit#theme-contract). If a new role is necessary:

1. Name it after purpose rather than hue or a single screen.
2. Add values to
   `src/main/webapp/content/scss/themes/_default-variables.scss` and
   `_dark-variables.scss`.
3. Check text and non-text contrast on every surface where the role is used.
4. Add a semantic Tailwind mapping only when application markup needs a utility.
5. Add a `--tumaet-ui-*` mapping only when package components need that supported customization
   axis; update the package reference theme and validation with it.
6. Verify the real component in both themes, compact layouts, forced-colors mode, and its relevant
   interaction states.

Avoid theme-specific component selectors in `theme-default.scss` or `theme-dark.scss`. They are a
last resort for legacy global integrations. A new application or TUM UI component should consume
semantic properties instead.

## Reading and changing the active theme

CSS should normally react through `data-theme` and semantic properties without TypeScript. When
behavior genuinely depends on the active theme, consume the `ThemeService.currentTheme` signal:

```ts
private readonly themeService = inject(ThemeService);

protected readonly isDark = computed(() => this.themeService.currentTheme() === Theme.DARK);
```

Code that reads resolved CSS values after a stylesheet swap, such as a canvas chart, must also
depend on `ThemeService.appliedThemeRevision`. This signal changes only after the selected theme
stylesheet is active.

Call `applyThemePreference()` only in response to an explicit user choice. Pass `undefined` to
restore system-preference mode:

```ts
this.themeService.applyThemePreference(Theme.DARK);
this.themeService.applyThemePreference(undefined);
```

Do not subscribe manually to theme changes or mirror the current theme into a mutable field.

## Migration coexistence

Bootstrap is still global, and PrimeNG remains in screens whose TUM UI capability is incomplete.
Their cascade and source-scanning constraints are documented in
[Client Development: Styling](/developer/guidelines/client-development#styling).
