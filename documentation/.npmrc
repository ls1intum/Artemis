# Keep an npm-like hoisted node_modules so patch-package's `gray-matter` patch
# resolves at top-level node_modules/<pkg>/ in CI. Mirror the policy in the
# project root .npmrc.
shamefully-hoist=true
public-hoist-pattern[]=*
prefer-frozen-lockfile=true
enable-pre-post-scripts=true
