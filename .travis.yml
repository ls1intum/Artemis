language: java
jdk: openjdk8

env: NODE_VERSION=10.14.2


cache:
  yarn: true
  directories:
    - $HOME/.m2
    - $HOME/.gradle

before_install: nvm install $NODE_VERSION

script: ./gradlew -Pprod bootWar
