# Inline Structure

Text with *emphasis*, **strong emphasis** and `inline code`, plus an escaped bracket \[not a task]
and an escaped asterisk \*not emphasis\*.

1. [task][Parse input](testParseInput())
   - A nested bullet before the nested task.
   - Prefix text [task][Validate input](testValidateInput()) suffix text.
   - A nested bullet after the nested task.
2. [task][Report results](testReportResults())

Read the [linked guide](/api/core/files/markdown/guide.pdf) and the
[external reference](https://docs.example.org/guide?a=1&b=2) before starting.

![Local diagram](/api/core/files/markdown/overview.png "Overview")

![Remote badge](https://img.example.org/badge.svg)

- Unordered item with an [inline link](https://example.org/one).
- Unordered item with an escaped pipe \| character.
