import { Component } from '@angular/core';

/** Stands in for the TUM AET UI confirm dialog so a spec can assert on the confirmation service alone. */
@Component({
    selector: 'tumaet-ui-confirm-dialog',
    template: '',
})
export class ConfirmDialogStubComponent {}
