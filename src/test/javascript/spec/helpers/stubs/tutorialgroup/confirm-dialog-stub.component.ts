import { Component } from '@angular/core';

/** Stands in for the TUM UI confirm dialog so a spec can assert on the confirmation service alone. */
@Component({
    selector: 'tum-ui-confirm-dialog',
    template: '',
})
export class ConfirmDialogStubComponent {}
