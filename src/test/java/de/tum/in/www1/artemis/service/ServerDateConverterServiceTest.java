package de.tum.in.www1.artemis.service;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;

public class ServerDateConverterServiceTest {

    @Autowired
    private static ServerDateConverterService service;

    @Test
    public void testEmptyDate() {

    }

    /*
     * it('Return empty string if given date time is null', () => { let localizedDateTime = pipe.transform(null); expect(localizedDateTime).to.be.equal(''); localizedDateTime =
     * pipe.transform(moment(null)); expect(localizedDateTime).to.be.equal(''); });
     */

}
