apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bitbucket
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path
  volumeMode: Filesystem