apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: artemis-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  storageClassName: local-path
  volumeMode: Filesystem
