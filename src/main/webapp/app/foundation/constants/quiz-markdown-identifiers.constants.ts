/**
 * Markdown identifiers used to mark up quiz explanations and hints in raw markdown text.
 * Shared between the editor actions that insert them and the markdown parser that extracts them.
 */
export const QUIZ_EXPLANATION_IDENTIFIER = '[exp]';
export const QUIZ_HINT_IDENTIFIER = '[hint]';
