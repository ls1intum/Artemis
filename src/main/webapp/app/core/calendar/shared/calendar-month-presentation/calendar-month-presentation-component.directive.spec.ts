import { CalendarMonthPresentationComponent } from './calendar-month-presentation-component.directive';

describe('CalendarMonthPresentation', () => {
    it('should create an instance', () => {
        const directive = new CalendarMonthPresentationComponent();
        expect(directive).toBeTruthy();
    });

    // TODO: implement
});
