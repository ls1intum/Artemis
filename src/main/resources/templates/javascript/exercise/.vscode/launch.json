{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug App",
            "program": "${workspaceFolder}/src/client.js",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
