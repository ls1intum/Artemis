name: artemis_test
description: Artemis exercise tests
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.6.0

# Add regular dependencies here.
dependencies:
  lints: ^5.0.0
  test: ^1.24.0

  ${packageName}:
    path: "../${studentParentWorkingDirectoryName}"
