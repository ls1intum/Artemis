name: ${packageName}
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.6.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
