{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug App",
            "cargo": { "args": ["build", "--manifest-path", "${workspaceFolder}/Cargo.toml"] },
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
