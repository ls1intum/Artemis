{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug App",
            "program": "${workspaceFolder}/helloWorld.out",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build (debug)"
        }
    ]
}