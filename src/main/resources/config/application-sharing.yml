# ===================================================================
# sharing specific properties: this file will only be loaded during startup if the profile sharing is active
#
# This configuration extends the application.yml file for specific sharing configs.
# ===================================================================
artemis:
  sharing:
    # the shared common secret
    apikey: acdd-erdf-asd2-234f-234d-32eb
    # a url used to force reinitialization of Sharing Platform configuration
    # if not set, you have to wait for the next scheduled reinitialization request from the sharing platform
    # (typically every 10 minutes)
    serverurl: http://localhost:9001
    actionname: Export to Artemis@testInstance




