<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">
    <!-- changeset only needed to update to the same changelog as in #8284 -->
    <!-- TODO: remove when 7.0.0 initial schema is released -->
    <changeSet author="rstief" id="20240401142400">
        <addColumn tableName="knowledge_area">
            <!-- default value needed so the database does not break -->
            <column name="short_title" type="varchar(10)" value="1">
                <constraints nullable="false" />
            </column>
        </addColumn>
        <!-- default value needed so the database does not break -->
        <addNotNullConstraint columnName="knowledge_area_id" columnDataType="bigint" tableName="standardized_competency" defaultNullValue="1"/>
        <dropIndex indexName="idx_standardized_competency_title" tableName="standardized_competency"/>
    </changeSet>
</databaseChangeLog>
