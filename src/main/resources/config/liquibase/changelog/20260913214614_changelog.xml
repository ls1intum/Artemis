<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Course-level defaults for the Athena feedback style (detail/formality), on the same 1-3 scale as a student's
         own learner-profile preference. 0 means "no course default": a student without a preference of their own then
         falls back to the built-in default instead. -->
    <changeSet id="20260913214614-1-add-course-athena-config-feedback-style-defaults" author="musaberkay">
        <addColumn tableName="course_athena_config">
            <column name="default_feedback_detail" type="int" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="default_feedback_formality" type="int" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
        </addColumn>
    </changeSet>

</databaseChangeLog>
