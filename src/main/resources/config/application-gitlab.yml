artemis:
    gitlab:
        url: <GitLab URL>
        secret: <token>                 # GitLab API token giving Artemis full Admin access
        ci-token: <token from the CI>   # Token generated by the CI (e.g. Jenkins) for webhooks from GitLab to the CI
                                        # This token will always be included in triggered hooks to the CI
