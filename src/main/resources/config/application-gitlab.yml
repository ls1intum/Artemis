# ===================================================================
# GitLab specific properties: this file will only be loaded during startup if the profile gitlab is active
#
# This configuration overrides the application.yml file.
# ===================================================================
artemis:
    version-control:
        url: http://localhost:8081
        # user: PLEASE CHANGE THIS SECRET IN YOUR ENV OR application-local-secrets.yml
        # password: PLEASE CHANGE THIS SECRET IN YOUR ENV OR application-local-secrets.yml
        # token: PLEASE CHANGE THIS SECRET IN YOUR ENV OR application-local-secrets.yml
        # ci-token: PLEASE CHANGE THIS SECRET IN YOUR ENV OR application-local-secrets.yml
        # health-api-token: PLEASE CHANGE THIS SECRET IN YOUR ENV OR application-local-secrets.yml
        ssh-template-clone-url:
        ssh-keys-url-path: /-/profile/keys
        ssh-private-key-folder-path: /opt/keys
        # ssh-private-key-password: PLEASE CHANGE THIS SECRET IN YOUR ENV OR application-local-secrets.yml
        default-branch: main # The branch that should be used as default branch for all newly created repositories. This does NOT have to be equal to the default branch of the VCS
        versionControlAccessToken: true

gitlab:
    # The following (optional) parameter allows to enable the use of pseudonyms.
    # If activated, the user's first and last name are not transferred to gitlab.
    # Instead the user will be called "user <loginname>".
    # See also https://gitlab.com/gitlab-org/gitlab/-/issues/14123
    use-pseudonyms: false
